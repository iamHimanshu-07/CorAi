# Web Framework & Core Utilities
Flask==3.0.3
Werkzeug==3.0.4
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Login==0.6.3
Flask-WTF==1.2.1
email-validator==2.2.0
Flask-Limiter==3.8.0
SQLAlchemy==2.0.34
alembic==1.13.2
python-dotenv==1.0.1
gunicorn==23.0.0
reportlab==4.2.2

# Data Science & Machine Learning Core (heart-disease model)
numpy>=2.0.0
pandas>=2.2.3
scikit-learn>=1.5.0
joblib>=1.4.2

# ML Experimentation (Phase 1)
xgboost>=2.1.0
lightgbm>=4.5.0
shap>=0.46.0
imbalanced-learn>=0.12.0
matplotlib>=3.9.2
seaborn>=0.13.2

# Production Extras (Phase 2)
pydantic>=2.10.0
structlog==24.4.0
pytest==8.3.3
pytest-cov==5.0.0
ruff==0.7.2

# Additional Features Requirements
openai
pdfplumber
beautifulsoup4
requests
plotly
flask-cors

# ---------------------------------------------------------------------------
# RAG chatbot (HeartAI Copilot) — OPTIONAL on Render.
#
# The full RAG stack (langchain + langchain-classic + langchain-google-genai +
# faiss-cpu + sentence-transformers + google-generativeai) pulls in PyTorch and
# several hundred MB of native wheels. Render's free web service has a 512 MB
# RAM cap; importing sentence-transformers at boot OOM-kills gunicorn (exit
# 137) so it never binds $PORT, and Render logs "No open ports detected".
#
# To enable the chatbot on a host with more RAM (Render Starter+, Fly, a VPS,
# etc.), uncomment the block below and re-deploy. The chat route falls back
# to OpenAI (LLM_API_KEY) when RAG deps are missing.
#
# langchain>=1.0.0
# langchain-core>=1.0.0
# langchain-text-splitters>=1.0.0
# langchain-classic>=1.0.0
# langchain-community>=0.4.0
# langchain-google-genai>=2.0.0
# faiss-cpu>=1.8.0
# sentence-transformers>=3.0.0
# google-generativeai>=0.7.0
# ---------------------------------------------------------------------------

# FastAPI server (for standalone /api/chat) — not used by the gunicorn boot path.
fastapi>=0.110.0
uvicorn>=0.27.0
